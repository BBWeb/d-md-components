var Spinner = require('./../index');

/** 
 * Shows the spinner.
 */
Spinner.prototype.show = Spinner.prototype._show;

/** 
 * Hides the spinner.
 */
Spinner.prototype.hide = Spinner.prototype._hide;

